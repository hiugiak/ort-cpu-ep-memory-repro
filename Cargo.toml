[package]
name = "ort-cpu-ep-memory-repro"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
ort = { version = "=2.0.0-rc.11", default-features = false, features = ["load-dynamic"] }
rand = { version = "0.8", features = ["small_rng"] }
